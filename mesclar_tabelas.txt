let
    Fonte = Excel.CurrentWorkbook(){[Name="Table"]}[Content],
    #"Personalização Adicionada" = Table.AddColumn(Fonte, "Personalizar", each Excel.CurrentWorkbook(){[Name="TabelaPlanos"]}[Content]),
    #"Personalizar Expandido" = Table.ExpandTableColumn(#"Personalização Adicionada", "Personalizar", {"Plano", "Valor"}, {"Plano", "Valor"})
in
    #"Personalizar Expandido"
